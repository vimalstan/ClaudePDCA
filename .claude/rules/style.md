# Style Rules

- Follow project CLAUDE.md conventions — read before writing code
- No unnecessary comments, docstrings, or type annotations on unchanged code
- No speculative abstractions — build for actual requirements, not hypothetical ones
- Three similar lines is better than a premature abstraction
- No backwards-compatibility shims — if unused, delete completely
- Match existing patterns in the codebase before introducing new ones
