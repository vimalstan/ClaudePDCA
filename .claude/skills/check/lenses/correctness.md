# Correctness Lens

- Does this actually solve the stated problem?
- Are there logic errors, off-by-ones, or missing cases?
- Does the happy path work? Do all branches terminate correctly?
- Are invariants preserved?
- Do return values match the documented/expected types?
- Are error paths handled explicitly (not swallowed)?
